# ---------------------------------------------------------------------------
# Profil PROD — production
# ---------------------------------------------------------------------------
spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: WARN
    com.mobili.backend: INFO
    org.springframework.web: WARN
    org.hibernate.SQL: ERROR

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never

mobili:
  cors:
    allowed-origins:
      - https://my-mobili.com
      - https://www.my-mobili.com
      - capacitor://localhost
      - http://localhost
  security:
    refresh:
      secure: true

fedapay:
  secret:
    key: ${FEDAPAY_SECRET_KEY}
  webhook:
    secret: ${FEDAPAY_WEBHOOK_SECRET}